<!DOCTYPE html>
<html>
  <head>
    <title>RubyLlmDemo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Tailwind -->
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 text-gray-900">
    <!-- Navbar -->
    <header class="bg-white shadow fixed w-full top-0 z-50">
      <nav class="container mx-auto px-6 py-4 flex justify-between items-center">
        <%= link_to "RubyLLM Demo", root_path, class: "text-xl font-bold text-blue-600 hover:text-blue-700" %>

        <div class="space-x-6">
          <%= link_to "Chats", chats_path, class: "text-gray-700 hover:text-blue-600" %>
          <%= link_to "Models", models_path, class: "text-gray-700 hover:text-blue-600" %>
        </div>
      </nav>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto mt-28 px-6">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="mt-12 border-t py-6 text-center text-gray-600 text-sm">
      &copy; <%= Time.current.year %> RubyLLM Demo. Built with Rails & Tailwind.
    </footer>
  </body>
</html>
